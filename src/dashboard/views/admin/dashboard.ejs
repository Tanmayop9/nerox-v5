<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Nerox Dashboard</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/navbar', { username, role }) %>
    
    <main class="container">
        <div class="page-header">
            <h1>🛡️ Admin Dashboard</h1>
            <p>Manage all bot databases</p>
        </div>
        
        <div class="admin-stats">
            <h2>📊 Database Overview</h2>
            <div class="stats-grid admin-grid">
                <% for (const db of databases) { %>
                    <div class="stat-card admin-card">
                        <div class="stat-info">
                            <h3><%= db %></h3>
                            <p class="stat-value"><%= stats[db] || 0 %> entries</p>
                        </div>
                        <a href="/admin/database/<%= encodeURIComponent(db) %>" class="btn btn-small btn-admin">Manage</a>
                    </div>
                <% } %>
            </div>
        </div>
        
        <div class="admin-actions">
            <h2>⚡ Quick Actions</h2>
            <div class="action-buttons">
                <a href="/admin/bulk" class="btn btn-primary">📦 Bulk Operations</a>
                <a href="/admin/database/blacklist" class="btn btn-danger">⛔ Manage Blacklist</a>
                <a href="/admin/database/botstaff" class="btn btn-success">👑 Manage Bot Staff</a>
            </div>
        </div>
    </main>
</body>
</html>
